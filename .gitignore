# Secrets — never commit
.env
.env.local
*.pem

# Dependencies
/node_modules
/.pnp
.pnp.js

# Build output
/tmp
/out-tsc
/dist

# Playwright
/test-results
/playwright-report
/blob-report
/playwright/.cache
results.xml

# Allure
/allure-results
/allure-report

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Editors / OS
.vscode/*
.idea/
.DS_Store

# Rate-limit snapshot between test run and report generation
.rate-limit-snapshot.json

# Allure history (persisted via CI cache, not committed)
allure-history.jsonl
